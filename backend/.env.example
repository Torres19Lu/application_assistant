# 数据库配置
# 本地开发使用 SQLite（推荐，无需额外安装）
DATABASE_URL=sqlite:///./master_application.db
# 生产环境使用 MySQL，取消下方注释并注释掉上方 SQLite 配置
# DATABASE_URL=mysql+pymysql://root:password@localhost:3306/master_application

# JWT配置
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# 应用配置
APP_NAME=xmum留学
DEBUG=True

# 文件上传配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
